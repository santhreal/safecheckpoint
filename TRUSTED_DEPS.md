# Trusted dependencies

Each external dependency is pinned to an exact version in `Cargo.toml` and audited before it appears here. Path dependencies are internal Santh crates and are not listed.

- blake3
- byteorder
- crc32fast
- criterion
- fs4
- memmap2
- proptest
- rayon
- serde
- serde_json
- tempfile
- tracing
