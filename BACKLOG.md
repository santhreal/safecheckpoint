# BACKLOG - safecheckpoint
src/shard.rs:94 | bug | high | num_shards == 0 causes division by zero panic in div_ceil when partitioning tensors | FIXED: guard num_shards==0 at top of save_sharded, return new Error::InvalidArgument{parameter,reason} (added variant to error.rs + Display). Proving test tests/unit/test_shard.rs::test_save_sharded_zero_shards_errors_not_panics (asserts Err(InvalidArgument) + no index file written). | status=done
tests/property/test_roundtrip.rs:53 | test-gap | high | test_property_roundtrip_proptest always panics on save because random data length doesn't match shape product | FIXED: prop_flat_map sizes the data vec to shape.product()*dtype.size_in_bytes() so save() accepts every tensor; stale "accepts arbitrary data" comment corrected. | status=done
tests/property/test_depth_property.rs:83 | test-gap | high | test_tensor_metadata_validation always fails because metadata strategy generates mismatching shapes and offsets | FIXED: tensor_metadata_strategy now derives data_offsets end = start + shape.product()*dtype.size (matches validate()); also keeps test_checkpoint_header_roundtrip valid. | status=done
tests/adversarial/test_depth_adversarial.rs:34 | test-gap | high | test_path_traversal_in_save expects Error::Io but save returns Error::PathTraversal causing panic | FIXED: assert Error::PathTraversal{reason} (the correct variant; code was right, test was stale). | status=done
tests/adversarial/test_depth_integer_overflow.rs:71 | test-gap | high | test_integer_overflow_offsets panics on Reader::open unwrap because header checksum is missing | FIXED: added shared helper adversarial::write_checkpoint_with_checksum (real blake3(header_len‖json), ONE-PLACE for all 3 crafted-file tests, +blake3 dev-dep); test now asserts get_tensor -> OffsetOverflow/SizeMismatch (real variant, not just is_err). | status=done
tests/adversarial/test_malformed.rs:52 | test-gap | high | test_invalid_json_header asserts Error::Json but gets Error::InvalidFormat due to missing 32-byte checksum | FIXED: write valid checksum over invalid JSON via shared helper so Reader::open reaches serde parse -> Error::Json. | status=done
tests/adversarial/test_malformed.rs:85 | test-gap | high | test_data_offset_overflow panics on Reader::open unwrap due to missing 32-byte header checksum | FIXED: shared helper writes valid checksum; asserts OffsetOverflow/SizeMismatch. | status=done
tests/adversarial/test_depth_adversarial.rs:113 | test-gap | high | test_truncated_file constructs invalid tensor (shape[1] F32 vs 1024 bytes) so save()? fails at size-validation before the truncation it means to test | FIXED: shape [256] F32 = 1024 bytes (valid), so it reaches the truncate+SizeMismatch check. | status=done
tests/adversarial/test_depth_adversarial_boundaries.rs:66 | test-gap | high | test_hash_collision_maximization asserts a wrong last-tensor key (29 A's vs 9999%100=99) so get_tensor errors | FIXED: reconstruct first/last keys with the writer's exact format!(...) formula. | status=done
tests/adversarial/test_depth_fault_injection.rs:1 | test-gap | critical | all 4 faultkit injection tests are permanently red fiction: faultkit is a DEV-only cooperative injector whose should_fail_* hooks are never called from the production lib, so injected Alloc/Write/Read/Mmap faults are never observed and assert!(is_err) can never pass | FIXED: rewrote to trigger REAL portable root-safe failures - save into non-dir parent (ENOTDIR Io), open truncated<8B (InvalidFormat), open a directory (mmap fails Io), oversized-header DoS (>100MB rejected pre-alloc); dropped the faultkit fiction. | status=done
src/shard.rs:117 | perf | high | data.clone() in save_sharded loop duplicates full tensor data in memory | VERIFIED real but needs Writer API change: add_tensor takes owned Vec<u8>, so shard_writer must own the bytes; borrowing requires Writer to store Cow/&[u8] tensor data (invasive, touches save/prepare_header). Substantial refactor (Writer data-ownership change), not a one-liner; left open for a dedicated pass. | status=done | FIXED via ONE-PLACE serialization refactor (better than the Cow-storage idea, which would have added a lifetime to Writer and infected every use site): introduced pub(crate) type TensorView<'a>=(&str,DType,&[usize],&[u8]) and factored serialization into build_header + write_tensors_to_file + save_borrowed, all operating over a borrowed &[TensorView]. Writer::save now builds sorted borrowed views over self.tensors (no clone) and delegates; save_sharded builds views straight out of sorted_tensors[start..end] and calls Writer::save_borrowed. The per-shard throwaway Writer + full-tensor data.clone() (potentially multi-GB per tensor) is GONE. Whole-file save and every shard share one serialization primitive (ONE PLACE). All 59 tests green incl test_save_load_sharded / test_resume_sharded / test_concurrent_stress_sharded.
src/writer.rs:202 | bug | medium | current_offset + data.len() computed without overflow check | FIXED: current_offset.checked_add(data.len()) -> Error::OffsetOverflow{tensor_name,offset}. (Not reachable with in-memory data since all tensors are allocated, but hardens the file-format offset computation per boundary-validation standard.) | status=done
src/writer.rs:248 | bug | medium | total_size computed with unchecked addition can overflow u64 | FIXED: chained checked_add (8 + header_len + current_offset + 32) -> Error::OffsetOverflow. | status=done
src/writer.rs:173 | bug | low | Directory sync is silently skipped for relative paths with no parent directory components | FIXED (worse than described: bare filename parent() is Some("") not None, so open("") FAILED, breaking bare-filename saves AND the tempfile base). Normalized empty parent to "." in the single `base` derivation and reused `base` for the fsync barrier. Proving test tests/unit/test_writer.rs::test_writer_save_bare_relative_filename_is_durable (chdir into tempdir, save+read bare filename). | status=done
src/writer.rs:121 | safecheckpoint | silent-fallback | low | REPOPULATE 2026-07-16: symlink_metadata fails on a path component, the resulting error is silently discarded, bypassing symlink check validation for that component | surface loudly by propagating any error from symlink_metadata other than NotFound, and a proving test asserting that a path returning permission-denied on metadata check returns an error during save | FIXED (Law-10 fail-closed): validate_write_path's silent symlink-component loop (which swallowed every symlink_metadata error and continued) is replaced by module-level `reject_symlink_components<F: FnMut(&Path) -> io::Result<Metadata>>(leaf, stat)`. It fails CLOSED: an Ok symlink -> PathTraversal; `Err(NotFound)` -> tolerated (component not yet created); ANY OTHER Err (permission-denied, ENOTDIR, ...) -> PathTraversal("could not verify path component is not a symlink: {source}"). The `stat` closure is injectable so the fail-closed path is deterministically testable without root/chmod. Proving tests reject_symlink_components_fails_closed_on_permission_denied (injected PermissionDenied on a component -> Err) and reject_symlink_components_tolerates_not_found (injected NotFound -> Ok). Gate: cargo test -p safecheckpoint 59+1 passed, EXIT=0. | status=done